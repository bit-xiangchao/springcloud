server:
  context-path: /uaa
  port: 5000

management:
  security:
    enabled: false
  context-path: /mgmt

eureka:
  instance:
    health-check-url-path: ${server.context-path}${management.context-path}/health
    status-page-url-path: ${server.context-path}${management.context-path}/info
    metadata-map:
      management.context-path: ${server.context-path}${management.context-path}

spring:
  datasource:
    url: jdbc:mysql://219.141.189.170:3306/springbootdb?useUnicode=true&characterEncoding=utf8
    admin: admin
    password: ctbri4008118114
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
